// Types for static image imports (import logo from "…/logo.webp").
// Normally provided by the generated next-env.d.ts, which is not committed:
// this file keeps `tsc --noEmit` working on a clean checkout (CI).
/// <reference types="next/image-types/global" />
